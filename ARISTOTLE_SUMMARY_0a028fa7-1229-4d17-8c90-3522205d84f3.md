# Summary of changes
## Summary of work completed

### Focus: Hammersley-Welsh formalization and blueprint updates

#### New file: `RequestProject/SAWZigzagBuild.lean`
Created a new file providing the core infrastructure for the zigzag SAW construction, with the following **fully proved** (sorry-free) results:

1. **`buildZigzagWalk`** — Recursive construction of zigzag walks on hexGraph from binary choice lists
2. **`zigzagSAWWalk`** / **`zigzagSAWWalk_length`** — The walk from hexOrigin with length 2k for k choices
3. **`buildZigzagWalk_support_structure`** — Support of the zigzag walk has the expected structure
4. **`buildZigzagWalk_isPath`** — The zigzag walk is self-avoiding (key lemma!)
5. **`zigzag_foldl_injective`** — Single-step zigzag is injective in the choice
6. **`zigzag_scanl_injective`** — The full position sequence (scanl) is injective in the choices
7. **`zigzagToSAW`** — Packages the zigzag walk as a proper SAW
8. **`finToList_injective`** — `List.ofFn` is injective
9. **`saw_count_even_lower_proved`** — Partially proved: the injection structure (Fin k → Bool → SAW) is set up, with one remaining sorry for the dependent type cast injectivity

#### Updates to `RequestProject/SAWZigzag.lean`
- Proved **`scanl_zigzag_sum`** — The key zigzag invariant: position i in the scanl has x+y = start.x + start.y - i (by induction on choices, generalizing position and index)
- Cleaned up `zigzag_positions_nodup` to use the new invariant
- Added adjacency helper `hex_adj_T_choice` and `zigzag_step_sum`
- Fixed `saw_count_sq_ge_two_pow'` proof (already worked from the sorry'd even/odd bounds)

#### Blueprint updates (`blueprint/src/content.tex`)
Added comprehensive documentation for the Hammersley-Welsh proof structure:

1. **Bridge decomposition injection (core)** — Expanded with the detailed 4-step proof outline from the paper (half-plane walk decomposition by induction on width, general walk decomposition, injectivity via reverse procedure, weight accounting)
2. **Hammersley-Welsh abstract decomposition results** — Added entries for:
   - `recurrence_from_strip` (proved)
   - `quadratic_recurrence_lower_bound` (proved)
   - `harmonic_not_summable` (proved)
   - `not_summable_of_lower_bound` (proved)
   - `case2_divergence` (proved)
3. **Zigzag construction** — Added entries for:
   - `scanl_zigzag_sum` (proved)
   - `zigzag_positions_nodup` (proved)
   - `buildZigzagWalk_isPath` (proved)
   - `zigzag_scanl_injective` (proved)

#### Remaining sorries (unchanged from before, except new partial proof)
The main remaining gaps are:
- `bridge_decomposition_injection_proof` (SAWHWDecomp.lean) — Core HW counting inequality
- `strip_identity_concrete` (SAWFiniteStrip.lean) — The parafermionic observable identity
- `origin_bridge_upper_bound` / `origin_bridge_lower_bound` (SAWBridgeFix.lean) — Depend on strip identity
- `saw_count_even_lower` / `saw_count_odd_lower` (SAWZigzag.lean) — Zigzag counting (infrastructure now in place in SAWZigzagBuild.lean)

All files compile without errors.