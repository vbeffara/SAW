# Summary of changes
## Summary of Changes

### Key Improvements

1. **Fixed circular dependency in bridge decay** (`RequestProject/SAWPaperChain.lean`):
   - `paper_bridge_decay` now uses partial sum bounds (`paper_bridge_partial_sum_le` → `Real.tsum_le_of_sum_le`) instead of the sorry'd `hammersley_welsh_injection` from `SAWBridgeFix.lean`.
   - This breaks the circular dependency where bridge decay needed HW summability and HW summability needed bridge decay.

2. **Proved `hw_summable_corrected`** (`RequestProject/SAWPaperChain.lean`):
   - Proved that Z(x) < ∞ for 0 < x < x_c using:
     - `paper_bridge_decomp_injection` (sorry'd — the HW decomposition)
     - `paper_bridge_decay` (proved — no circular dependency)
     - Geometric product convergence bound
   - The proof goes through the corrected paper bridge chain, not the false x-coordinate chain.

3. **Marked false/superseded theorems** (`RequestProject/SAWBridgeFix.lean`):
   - `origin_bridge_upper_bound` is **FALSE** for x-coordinate bridges (for T=1, the sum ≈ 1.76 > 1)
   - `origin_bridge_lower_bound`, `Z_xc_diverges`, `hammersley_welsh_injection` are marked as superseded by the corrected diagonal bridge chain
   - Comments explain why these are wrong and point to the corrected versions

4. **Updated blueprint** (`blueprint/src/content.tex`, `blueprint/lean_decls`):
   - Updated `paper_bridge_decay` (renamed from `paper_bridge_decay_corrected`)
   - Added `hw_summable_corrected` theorem and proof to blueprint
   - Updated main theorem dependencies and sorry status

5. **Updated proof status** (`PROOF_STATUS.md`):
   - Clear documentation of the 3 remaining sorry's on the critical path
   - Documentation of all proved infrastructure

### Current Sorry Status

The main theorem `connective_constant_eq_corrected` (μ = √(2+√2)) depends on exactly **3 sorry's**:

1. **`B_paper_le_one_direct`** (SAWStripIdentityCorrect.lean) — The fundamental gap: B_paper(T,L,x_c) ≤ 1. Requires the full parafermionic observable argument (vertex relation → discrete Stokes → boundary evaluation). All algebraic ingredients are proved.

2. **`paper_bridge_lower_bound`** (SAWPaperChain.lean) — ∃ c > 0, paper_bridge_partition(T, x_c) ≥ c/T. Depends on #1. Abstract quadratic recurrence infrastructure exists.

3. **`paper_bridge_decomp_injection`** (SAWPaperChain.lean) — The Hammersley-Welsh bridge decomposition: ∑ c_n x^n ≤ 2·(∑_S ∏ B_{T+1})². Independent of #1. Requires formalizing the walk decomposition algorithm.

### Files Modified
- `RequestProject/SAWPaperChain.lean` — Main changes (decay fix, hw_summable proof)
- `RequestProject/SAWBridgeFix.lean` — Marked false/superseded theorems  
- `blueprint/src/content.tex` — Updated blueprint
- `blueprint/lean_decls` — Updated declaration list
- `PROOF_STATUS.md` — Updated status documentation
